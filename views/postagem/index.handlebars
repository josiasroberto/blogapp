<div class="card">
  <div class="card-body">
    <h1>{{postagem.titulo}}</h1>
    <h6 class="m-2">{{postagem.descricao}}</h6>
    <hr>
    <small>Data de publicação: {{postagem.data}}</small>
    <hr>
    <p>{{postagem.conteudo}}</p>
  </div>
</div>